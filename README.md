# Loan-Risk-Assessment-App
A Streamlit app that asks a loan applicant simple, plain-language questions one at a time, then uses your trained Gaussian Naive Bayes model (naive_bayes_model.pkl) to estimate the risk that the loan would default.
